name: Cypress E2E Tests via Ngrok

on: workflow_dispatch

jobs:
  cypress-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with: { node-version: '18' }

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests against Ngrok tunnel
        run: |
          # 🔑  Paste your ngrok tunnel + path prefix here:
          NG='https://f89aa2136e9b.ngrok-free.app/UL_SavingsAccount-API_prototype'
          echo "✅ baseUrl=$NG"
          npx cypress run --config baseUrl=$NG
